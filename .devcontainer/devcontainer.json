{
  "name": "medmcp-neuro-core",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "..",
    "args": { "BASE_IMAGE": "medmcp-base:dev" }
  },
  "workspaceFolder": "/workspaces/medmcp-neuro-core",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/medmcp-neuro-core,type=bind",
  "mounts": [
    "source=${localEnv:XDG_RUNTIME_DIR}/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "containerEnv": {
    "UV_LINK_MODE": "copy",
    "UV_NATIVE_TLS": "1"
  },
  "postCreateCommand": "uv sync",
  "remoteUser": "root"
}
